global.Buffer = global.Buffer || require("buffer").Buffer;

// if (__meteor_runtime_config__ && __meteor_runtime_config__.VUE_DEV_SERVER_URL) {
  // __meteor_runtime_config__.VUE_DEV_SERVER_URL = "localhost:3030";
// }
